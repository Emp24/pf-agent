# Python bytecode & caches
__pycache__/
*.py[cod]

# Virtual environment (regenerable from uv.lock — never commit)
.venv/

# Tooling caches
.ruff_cache/
.pytest_cache/

# Build / packaging artifacts
dist/
build/
*.egg-info/

# Secrets & local env (API keys live here — never commit)
.env
.env.*

# macOS
.DS_Store
